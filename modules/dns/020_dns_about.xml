<para><command>dns</command> is a fundamental part of every large computer network. <command>dns</command> is used by many network services to translate names into network addresses and to locate services on the network (by name).</para>
<para>Whenever you visit a web site, send an e-mail, log on to Active Directory, play Minecraft, chat, or use VoIP, there will be one or (many) more queries to <command>dns</command> services.</para>
<para>Should <command>dns</command> fail at your organization, then the whole network will grind to a halt (unless you hardcoded the network addresses).</para>
<para>You will notice that even the largest of organizations benefit greatly from having one <command>dns</command> infrastructure. Thus <command>dns</command> requires all business units to work together.</para>
<para>Even at home, most home modems and routers have builtin <command>dns</command> functionality.</para>
<para>This module will explain what <command>dns</command> actually is and how to set it up using <command>Linux</command> and <command>bind9</command>.</para>
