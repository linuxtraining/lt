	<section><title>Practice: discover the shell</title>
		<para>1. Create a testdir and enter it.</para>
		<para>2. Create files file1 file10 file11 file2 File2 File3 file33 fileAB filea fileA fileAAA file( file 2 (the last one has 6 characters including a space)</para>
		<para>3. List (with ls) all files starting with file</para>
		<para>4. List (with ls) all files starting with File</para>
		<para>5. List (with ls) all files starting with file and ending in a number.</para>
		<para>6. List (with ls) all files starting with file and ending with a letter</para>
		<para>7. List (with ls) all files starting with File and having a digit as fifth character.</para>
		<para>8. List (with ls) all files starting with File and having a digit as fifth character and nothing else.</para>
		<para>9. List (with ls) all files starting with a letter and ending in a number.</para>
		<para>10. List (with ls) all files that have exactly five characters.</para>
		<para>11. List (with ls) all files that start with f or F and end with 3 or A.</para>
		<para>12. List (with ls) all files that start with f have i or R as second character and end in a number.</para>
		<para>13. List all files that do not start with the letter F.</para>
		<para>14. Copy the value of $LANG to $MyLANG.</para>
		<para>15. Show the influence of $LANG in listing A-Z or a-z ranges.</para>
		<para>16. Write a command line that executes 'rm file55'. Your command line should print 'success' if file55 is removed, and print 'failed' if there was a problem.</para>
		<para>17. You receive information that one of your servers was cracked, the cracker probably replaced the ls command. You know that the echo command is safe to use. Can echo replace ls ? How can you list the files in the current directory with echo ?</para>
		<para>18. The cd command is also compromised, can echo be used to list files in other directories ? Explain how this works (list the contents of /etc and /bin without ls). </para>
		<para>19. Is there another command besides cd to change directories ?</para>
		<para>20. Make sure bash remembers the last 5000 commands you typed.</para>
		<para>21. Open more than one console (press Ctrl-shift-t in gnome terminal) with the same user account. When is command history written to the history file ?</para>
		<para>22. Issue the date command. Now display the date in YYYY/MM/DD format.</para>
		<para>23. Issue the cal command. Display a calendar of 1582 and 1752. Notice anything special ?</para>
		<para></para>
		<para></para>	
	</section>
