<section><title>mysql user management</title>
	<section><title>Linux users</title>
		<para>The installation of <command>mysql</command> creates a user account in <command>/etc/passwd</command><indexterm><primary>/etc/passwd</primary></indexterm> and a group account in <command>/etc/group</command><indexterm><primary>/etc/group</primary></indexterm>.</para>
		<screen>
paul@barry:~$ grep mysql /etc/passwd
mysql:x:103:105:MySQL Server,,,:/var/lib/mysql:/bin/false
paul@barry:~$ grep mysql /etc/group
mysql:x:105:
		</screen>
		<para>The mysql daemon <command>mysqld</command><indexterm><primary>mysqld</primary></indexterm> will run with the credentials of this user and group.</para>
	</section>
	<section><title>mysql root user</title>
		<para>With the <command>mysqladmin</command><indexterm><primary>mysqladmin</primary></indexterm> command you can create a root user to administer your mysql server. It is advised to use a different password than the one for your Unix root account. After doing this, any user can issue mysql root commands using the <command>-u root</command> argument on the commandline.</para>
		<screen>root@laika:~# mysqladmin -u root password SecretPass</screen>
	</section>
	<section><title>mysql client application</title>
		<para>You can now use mysql from the commandline by just typing <command>mysql -u root -p</command><indexterm><primary>mysql</primary></indexterm> and you 'll be asked for the password (of the mysql root account). In the screenshot below, the welcome message is cut.</para>
		<screen>
# Linux barry 2.6.17-2-686 #1 SMP Wed Sep 13 16:34:10 UTC 2006 i686 GNU/Linux
paul@barry:~$ mysql -u root -p
Enter password:
&#060;cut MySQL Welcome Message&#062;
mysql&#062; exit
Bye
		</screen>
		<para>You could also put the password in clear text on the command line, but that would not be very secure. Anyone with access to your bash history would be able to read your mysql root password.</para>
		<screen>
paul@barry:~$ mysql -u root -pSecretPass
&#060;cut MySQL Welcome Message&#062;
mysql&#062; exit
Bye
		</screen>
	</section>
	<section><title>~/.my.cnf</title>
		<para>You can save configuration in your home directory in the hidden file <command>.my.cnf</command><indexterm><primary>.my.cnf</primary></indexterm>. In the screenshot below we put the root password in .my.cnf.</para>
		<screen>
paul@barry:~$ echo "[client]" &#062; .my.cnf
paul@barry:~$ echo password=SecretPass &#062;&#062; .my.cnf
paul@barry:~$ cat .my.cnf
[client]
password=SecretPass
paul@barry:~$ mysql -u root
&#060;cut MySQL Welcome Message&#062;
mysql&#062;
		</screen>
	</section>
	<section><title>the mysql command line client</title>
		<para>You can use the <command>mysql</command><indexterm><primary>mysql</primary></indexterm> command to take a look at the databases, and to execute SQL queries on them. The screenshots below show you how. First, we log on to the mysql server.</para>

		<screen>
paul@laika:~$ mysql -u root -p
Enter password: 
Welcome to the MySQL monitor.  Commands end with ; or \g.
Your MySQL connection id is 8
Server version: 5.0.51a-3ubuntu5.4 (Ubuntu)

Type 'help;' or '\h' for help. Type '\c' to clear the buffer.
mysql> 
		</screen>
		<para>Next, we execute the command <command>show databases</command>. Every command must be terminated by a delimiter. The default delimiter is <command>;</command> (the semicolon).</para>
		<screen>
mysql> show databases;
+--------------------+
| Database           |
+--------------------+
| information_schema | 
| demodb             | 
| mysql              | 
| netsec             | 
| test               | 
+--------------------+
5 rows in set (0.00 sec)
		</screen>
		<para>Type <command>exit</command> to get back to your Linux command prompt.</para>
		<screen>
mysql> exit
Bye
paul@laika:~$ 
		</screen>
	</section>
</section>
