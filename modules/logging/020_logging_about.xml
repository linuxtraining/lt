<para>This chapter has three distinct subjects.</para>
<para>First we look at login logging ; how can we find out who is logging in to the system, when and from where. And who is not logging in, who fails at <command>su</command> or <command>ssh</command>.</para>
<para>Second we discuss how to configure the syslog daemon, and how to test it with <command>logger</command>.</para>
<para>The last part is mostly about <command>rotating logs</command> and mentions the <command>tail -f</command> and <command>watch</command> commands for <command>watching logs</command>.</para>
